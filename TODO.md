# TODO — відкладені покращення

Знайдено під час дизайн/адаптив-аудиту (комміти `c327ff1`, `14f1c12`).
Це не критичні баги — сайт працює. Кожен пункт свідомо відкладено, бо
потребує зміни розмітки/збірки/контенту, а не адаптивної правки.

---

## 1. Full-bleed секції на моніторах ширших за 1408px

**Що:** головний контейнер обмежено `max-width: 1408px` (`#src/style/css/style.css`
→ `.__container`; футер — `#src/style/css/style_footer.css` → `.footer__container`;
змінна `--header-width-container` у `#src/style/css/root.css`). На екранах ширших
за 1408px секції, які мали б тягнутися від краю до краю (темна смуга «Facilities»,
фото-герої, бежеві блоки), обриваються, і по боках з'являються білі поля.

**Де видно:** лише на моніторах > 1408px (напр. 1920px). На ≤1440px непомітно.

**Чому відкладено:** потрібно виносити ці блоки за межі `.__container` у розмітці
(або застосувати `margin-inline: calc(50% - 50vw)`), тобто перебудова структури,
а не CSS-правка.

**Ідея фікса:** для full-width-секцій додати клас на кшталт `_full-bleed` з
`width: 100vw; margin-left: calc(50% - 50vw);` і винести їх з-під центрального
контейнера, залишивши внутрішній вміст у `max-width: 1408px`.

---

## 2. «Malformed srcset» у `<picture>` — ВИРІШЕНО

Кома в `srcset` розділяла не fallback, а рівноцінних кандидатів, тож `.png`
і `.webp` були двома варіантами 1x і webp-оптимізація не працювала.

Закрито двома змінами:
1. рукописні `<picture>` у `#src` (хедер-лого, фото-герої, `with-love`)
   переписані на `<source type="image/webp">` + `<img>` як фолбек;
2. `gulp-webp-html` замінено на `gulp-webp-html-nosvg` — він генерує
   `<picture><source srcset="….webp" type="image/webp"><img src="….png"></picture>`,
   не підсовує `.svg`/`.gif` під `type="image/webp"` і не склеює html в один рядок.

---

## 3. Різнобій назви «Vesterbord» / «Vesterborg» — ВИРІШЕНО

Власник обрав варіант із картинки-мапи — `Vesterborg`. Текст і переклади
(en/da/uk/de) приведено до нього в `i18n-src/*.json`, `#src/*.html` і README;
українською — `Вестерборг` (місцевий відмінок `у Вестерборзі`).
Картинку мапи не чіпали — на ній підпис уже правильний.
